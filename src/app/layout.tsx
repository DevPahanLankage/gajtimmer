import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/Header'
import Footer from '@/components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'GaJTimmer - Premium Smartwatches',
  description: 'Discover durable and feature-rich smartwatches for active lifestyles. Health tracking, fitness monitoring, and professional-grade wearable technology.',
  keywords: 'smartwatch, fitness tracker, health monitoring, wearable technology, GaJTimmer',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.className} bg-black text-white antialiased`}>
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}
